apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: worker
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - image: krishmint/votingproj-workerapp:v2 
        name: worker
        env:
        - name: REDIS_HOST
          value: "10.47.5.235" 
        - name: POSTGRES_HOST
          value: "10.174.240.3"
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "Krish@0303"
        - name: POSTGRES_DB
          value: "postgres"
